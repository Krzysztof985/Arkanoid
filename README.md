# Arkanoid
Arkanoid game in C++ using .NET Framework. Installer for game itself, rest is the code for searching through in Visual Studio. If you want to compile files in Visual Studio first unlock them in a folder using file properties (right click on a file in file explorer, then properties, what you need is at the bottom).
